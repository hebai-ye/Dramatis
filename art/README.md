# 立绘候选集（待挑选）

打开 [编号总览图](review-portraits.jpg) 和 [面部总览图](review-faces.jpg)，按 `01`–`50` 标号决定保留、重画或删除。
`19` 与 `21` 已按要求删除，编号保留空位，现有 48 张。
角色命名按世界观区分：中式角色用中文姓名，日式角色用日式姓名，西幻角色用三个汉字左右的西式译名；
其他题材按设定命名。名称以 `apps/web/src/lib/portrait-catalog.json` 为准，修改后重跑资源准备脚本即可同步总览图。
原图在 `source/portraits/`，每张为 1024 × 1536 PNG；应用使用的完整 WebP 和缩略图分别在
`apps/web/public/portraits/` 与其 `thumbs/` 子目录。对话头像使用 `avatars/` 子目录的面部图，
角色卡编辑器中的图库也使用相同编号。用户上传的角色图和圆形裁切头像随角色卡保存在本地数据库中。

设计约束：14–30 岁、浅色系肤色、女性居多，女性以苗条身材为主、男性以修长身材为主，
允许少量丰满圆润的体型。人物面部偏现代日漫审美，画法与题材可不同；避免明显的国家旗帜符号。

重新准备图标、WebP、缩略图与总览图：`python tools/art/prepare-assets.py`。
