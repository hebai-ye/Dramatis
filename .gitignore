# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
out/
*.tsbuildinfo

# Rust / Tauri
src-tauri/target/
target/

# Capacitor / Android
apps/android/android/
.gradle/
*.keystore
*.jks
local.properties

# env & secrets — 绝不提交模型 API Key
.env
.env.*
!.env.example
*.pem
*.p12
secrets/

# local databases & user data
*.sqlite
*.sqlite3
*.db
*.db-journal
*.db-wal
data/
backups/
*.jsonl

# logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# editor & OS
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# test & coverage
coverage/
.nyc_output/

# 本机私有部署笔记（含服务器地址、域名、凭据线索）
deploy/LOCAL-NOTES.md

# 本地会话产物：agent worktree（各带一份 node_modules）与临时测试角色卡。
# 不忽略的话，一次 `git add -A` 会把 worktree 与真实角色卡一起提交进去。
.claude/
tmp-test-cards/

# 立绘源图：48 张 1024×1536 PNG，约 114 MB，只留在本机，不进仓库（用户 2026-09-26 裁定）。
# 仓库里保留 art/README.md、两张总览图、tools/art/prepare-assets.py；
# 应用实际使用的是 apps/web/public/portraits/ 下的 WebP 与缩略图。
art/source/
