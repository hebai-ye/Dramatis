import type { ReplyLength } from '../model/conversation.js';

/**
 * 回答长度与「别重复」的硬规矩（顺序 67e）。
 *
 * 来历：2026-09-25 用真实模型（DeepSeek）在侧边浏览器里跑了 178 轮质量长跑，
 * 实录算出来的漂移是——
 *
 * - 一条回复平均 227 字，前 30 条 171 字、后 30 条 325 字；最长 491 字；
 * - 回复里点自己名字的平均次数从 0.9 涨到 5.6（110/178 条一段里出现 ≥3 次），
 *   于是「她把杯子放下」被写成「秦娘把杯子放下」——旁白腔，不是人说话；
 * - 同一个动作/道具在一段里反复出现（柜台 79 条、抹布 40 条、账本 56 条命中）。
 *
 * 用户对这三条的裁定：**动作可以连着做几个不同的，但不许同一个动作重复**；
 * 长度要收紧，同时给用户一个「回答模式」（偏短 / 标准 / 偏长）自己选。
 * 所以这里只写规矩，不做硬截断——截断会把半句话切掉，那正是 67c 修过的毛病。
 */
export const REPLY_LENGTH_RULES: Record<ReplyLength, string> = {
  short: '回答长度（偏短）：这一轮只写两三句，总长不超过 60 字，说完就停。',
  normal: '回答长度（标准）：这一轮写三到五句，总长不超过 150 字；把这一件事说清就收住，不要为了凑字数铺陈。',
  long: '回答长度（偏长）：这一轮可以写到 400 字上下，但每一段都要推进新的事，不要重复已经交代过的内容。',
};

/**
 * 反重复规矩。**允许连续多个动作**（用户 2026-09-25 明确：只拒绝同一个动作重复多次），
 * 管的是三件事：同一个动作/道具不重复、动作句不拿自己的名字当主语、不复述已答过的事。
 */
export const NO_REPEAT_RULE = [
  '这一段可以连着做几个不同的动作，但同一个动作、同一件道具不要重复；同一个意思不要换句话再说一遍。',
  '动作句不要用你自己的名字当主语：写「她把杯子放下」，不要写「（角色名）把杯子放下」。',
  '已经答过的事不要复述，把篇幅留给这一轮的新内容。',
].join('\n');
